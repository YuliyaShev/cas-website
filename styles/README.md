# Styles

Global design tokens live in `app/globals.css` via Tailwind CSS v4's inline `@theme`.

Use this folder for additional shared CSS (e.g., tokens, utilities, or page-specific styles) as the site grows.
